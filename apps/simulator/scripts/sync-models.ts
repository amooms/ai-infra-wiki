import { syncHuggingFace } from './sync-huggingface'; import { syncModelScope } from './sync-modelscope'; import { normalizeMetadata } from './normalize-models'; import { buildModelIndex } from './build-model-index'
const results=await Promise.allSettled([syncHuggingFace(),syncModelScope()]);results.forEach((r,i)=>{if(r.status==='rejected')console.warn(`${i===0?'Hugging Face':'ModelScope'}: ${r.reason}`);else console.log(`${i===0?'Hugging Face':'ModelScope'} metadata: ${normalizeMetadata(r.value,i===0?'HuggingFace':'ModelScope').length}`)});console.log(`Validated seed/index records: ${await buildModelIndex()}`)
