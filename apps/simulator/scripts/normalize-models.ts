import type { Model } from '../src/types/schema'
export type UpstreamRecord={id?:string;modelId?:string;author?:string;lastModified?:string;tags?:string[]}
export function normalizeMetadata(rows:unknown,source:'HuggingFace'|'ModelScope'):Partial<Model>[] { if(!Array.isArray(rows))return[];return (rows as UpstreamRecord[]).map(row=>{const repo=row.id??row.modelId;return {model_id:repo?.toLowerCase().replace(/[^a-z0-9]+/g,'-'),display_name:repo?.split('/').pop(),organization:row.author??repo?.split('/')[0],sources:[source],source_repo:repo?(source==='HuggingFace'?`https://huggingface.co/${repo}`:`https://modelscope.cn/models/${repo}`):null,last_modified:row.lastModified??null,confidence:'low' as const,data_source:`${source} repository metadata; requires config enrichment`}}).filter(x=>x.model_id&&x.display_name) }
export function dedupeKey(m:Partial<Model>){return [m.organization,m.model_family,m.parameters_total,m.architecture].map(x=>String(x??'unknown').toLowerCase()).join('|')}
