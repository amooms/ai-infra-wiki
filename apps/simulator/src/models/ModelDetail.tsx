import type { Model, ModelVariant } from '../types/schema'; import { number } from '../utils/format'
export function ModelDetail({model,variant,onClose}:{model:Model;variant:ModelVariant;onClose:()=>void}) { const rows=[['Organization',model.organization],['Source',model.sources.join(' · ')],['Type',`${model.model_type} · ${model.moe?'MoE':'Dense'}`],['Total Parameters',number(model.parameters_total)],['Active Parameters',number(model.parameters_active)],['Layers',number(model.num_layers)],['Hidden Size',number(model.hidden_size)],['Attention / KV Heads',`${number(model.attention_heads)} / ${number(model.kv_heads)}`],['Head Dim',number(model.head_dim)],['Context Length',number(model.context_length)],['Precision',`${variant.weight_dtype} / ${variant.quantization_method??'none'}`],['Last Updated',model.last_modified??'N/A'],['Confidence',model.confidence],['Data Source',model.data_source],['Last Verified',model.last_verified??'N/A']]; return <div className="modal-backdrop" onClick={onClose}><article className="modal" onClick={e=>e.stopPropagation()}><button className="close" onClick={onClose}>关闭</button><p className="eyebrow">MODEL DETAIL</p><h2>{model.display_name}</h2><dl>{rows.map(([k,v])=><div key={k}><dt>{k}</dt><dd>{v}</dd></div>)}</dl>{model.source_repo&&<a href={model.source_repo} target="_blank" rel="noreferrer">查看 Source Repository ↗</a>}</article></div> }
